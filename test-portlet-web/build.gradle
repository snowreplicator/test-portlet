apply plugin: 'java'

compileJava {
    options.encoding = "UTF-8"
}

dependencies {
    compileOnly group: "com.liferay.portal", name: "release.portal.api"

    compileOnly project(":modules:test-portlet:test-portlet-api")
}

configurations {
    jar.archiveName = jar.baseName + '.jar'
}